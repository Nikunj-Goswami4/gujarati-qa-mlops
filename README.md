# gujarati-qa-mlops
A question-answering system that understands and answers questions in Gujarati, trained on a regional knowledge base, served via a FastAPI endpoint and Gradio UI, with a full MLOps pipeline (CI/CD, model versioning, drift monitoring, auto-retraining).
